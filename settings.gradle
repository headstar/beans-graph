def String[] modules = [
	'core'
]

include modules

rootProject.name = 'beangraph'

modules.each {name ->
	def p = findProject(":${name}")
	p.name = "${rootProject.name}-${name}"
	p.buildFileName = "${name}.gradle"
}

