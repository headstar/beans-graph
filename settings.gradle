def String[] modules = [
	'core'
]

include modules

rootProject.name = 'spring-bean-graph'

modules.each {name ->
	def p = findProject(":${name}")
	p.name = "${rootProject.name}-${name}"
	p.buildFileName = "${name}.gradle"
}

